var __index = {"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"index.html","level":1,"title":"Introducing <code>fetrunner</code>","text":"<p>This is primarily a tool for testing <code>FET</code> files. It runs multiple instances of <code>FET</code> (the command-line version) on a supplied <code>FET</code> file with various subsets of the constraints enabled. The aim is to assist in finding difficult (or impossible) constraints. In order to function as intended it needs to be able to run several processes in parallel – it should work with four processor cores, but better results are likely with at least six.</p> <p></p>","path":["Introducing fetrunner"],"tags":[]},{"location":"index.html#fet","level":2,"title":"<code>FET</code>","text":"<p><code>FET</code> is a free timetable generator program for educational establishments. It is widely used and very good at what it does. However, in the case of timetable data which \"doesn't work\" (because of conflicting constraints), it can sometimes be difficult to find where the problem lies. Also, with some data (lessons/activities and constraints) the calculation of a \"solution\" (a conflict-free timetable) can take a very long time. Whilst working on a timetable, it can be useful to know which constraints may be difficult to fulfil, without waiting a long time for <code>fet</code> to complete (or not ...).</p> <p><code>fetrunner</code> aims to produce a \"solution\" within a specified time, if necessary by deactivating some of the constraints. The result is a \"known working\" <code>FET</code> file (possibly including deactivated constraints). There is also a log file, which is updated continually during the process, showing some details of the progress, and a JSON file containing the activity placements from the \"successful\" <code>FET</code> run together with information about the \"failed\" constraints. In the GUI version of <code>fetrunner</code>, the log is not output as a file, but is used to update the interface (and is also available to view, if desired).</p>","path":["Introducing fetrunner"],"tags":[]},{"location":"index.html#using-fetrunner","level":2,"title":"Using <code>fetrunner</code>","text":"<p>Not running on Linux?</p> <p><code>fetrunner</code> produces many temporary files, which might cause excessive wear on an SSD. See Temporary files for ways to avoid this.</p>","path":["Introducing fetrunner"],"tags":[]},{"location":"index.html#gui-or-command-line","level":2,"title":"GUI or command-line","text":"<p>The GUI (Using the GUI) allows selection of <code>FET</code> files and processing parameters in a fairly straightforward way. It also shows the progress of a run dynamically. In some cases, however, the command-line tool may be more convenient. To use this, see Running the command-line tool.</p>","path":["Introducing fetrunner"],"tags":[]},{"location":"index.html#how-to-understand-the-results-of-a-fetrunner-run","level":2,"title":"How to understand the results of a <code>fetrunner</code> run","text":"<p><code>fetrunner</code> produces at least two result files in the same folder as the source file: from \"xxx.fet\", these would be \"xxx_Result.fet\" and \"xxx_Result.json\". The command-line version also produces a log file, \"xxx.log\". In the GUI version, the current log information is displayed in one of the tabs (older log information is not retained).</p> <p>If you open the \"xxx_Result.fet\" file in <code>FET</code>, you can see that some of the constraints have been deactivated, e.g. in the \"Time\" (and perhaps \"Space\") tab select \"All\". These are the ones which <code>fetrunner</code> decided were \"difficult\" or impossible.</p> <p>At the end of the log, there should be a summary of the accepted constraints.</p> <p>The \"xxx_Result.json\" file may contain some information about why a constraint was rejected, but it is rather intended for reading by other software. Nevertheless, a web browser (e.g. <code>Firefox</code>) can display this file quite neatly.</p> <p>The fact that <code>fetrunner</code> has deactivated a constraint doesn't mean that the constraint is necessarily impossible, though it may be (at least, in combination with other constraints). Another run, perhaps with a different timeout, might give a different result. The results show constraints whose removal makes it easier for <code>FET</code> to generate a timetable. If these constraints are important, it may be necessary to change other constraints which somehow interact with the shown ones – finding these may not be easy ...</p> <p>Looking at a timetable generated (in <code>FET</code>) from the \"xxx_Result.fet\" file (which is now known to be possible!) and at the deactivated constraints will – I hope – help you to discover how you might need to modify your data (activities and constraints) in order to get an acceptable result.</p> <p>Actually, there is a generated timetable (from \"xxx_Result.fet\") in \"xxx_Result.json\", but I haven't written the software to display this (yet).</p>","path":["Introducing fetrunner"],"tags":[]},{"location":"index.html#running-the-command-line-tool","level":2,"title":"Running the command-line tool","text":"<p>Important: By default the <code>FET</code> command-line executable is expected to be in the same directory as the <code>fetrunner</code> executable, or else runnable by calling <code>fet-cl</code> (on Windows the executable is <code>fet-cl.exe</code>), i.e. in the user's <code>PATH</code>. There is, however, a command line option (\"-fet\") to specify a different location – the value must be a full, absolute path.</p> <p><code>fetrunner</code> can be run with just the source file as argument:</p> <pre><code>./fetrunner path/to/fetfile.fet\n</code></pre> <p>This will normally run for up to five minutes, placing the results in the same directory as the source file, \"path/to/\" in the case of the above command:</p> <pre><code>`fetfile_Result.fet` – the `FET` file used to produce the result\n\n`fetfile_Result.json` – the results of the run, including the placements of the activities and the constraints which were deactivated\n\n`fetfile.log` – a log file giving information about the run\n\n`_fetfile.fet` – (optional, primarily for test purposes, with -xf option) should be essentially the same as the original `fetfile.fet`, but the constraints are indexed (in their \"Comments\" field) and the soft constraints are made \"hard\" (a derived weight, not the same as the original `FET` \"Weight_Percentage\" is added to the \"Comments\" field)\n</code></pre> <p>The log-file is updated continually during the run, so it is possible to monitor progress by reading this file.</p> <p>A run can be stopped prematurely by pressing <code>Ctrl-C</code>. This will probably take a couple of seconds to work, as it tries to tidy up. The result files will be produced from the current state.</p> <p>There are a few command-line options:</p> <pre><code>fetrunner -help\n -&gt;\n  -fet string\n        FET executable: /path/to/fet-cl\n  -h    skip hard constraint testing phase\n  -p int\n        max. parallel processes\n  -s    the weights of soft constraints are retained\n  -t int\n        set timeout, s (default 300)\n  -tmp string\n        Folder for temporary files (FET): /path/to/tmp\n  -v    print version and exit\n  -xd\n        run in debug mode\n  -xf\n        write fully-constrained FET file\n  -xt\n        run in testing mode\n</code></pre> <p>If it is known that the hard constraints are all satisfiable, the \"-h\" option can be used to always include the hard constraints (the unconstrained instance is not run) and test the sequential addition of just the soft constraints.</p>","path":["Introducing fetrunner"],"tags":[]},{"location":"GUI_details.html","level":1,"title":"Some details of the GUI code","text":"<p>The GUI acts as a front-end to the actual <code>fetrunner</code> code, which is included as a static library, <code>libfetrunner</code>. This library has a very simple interface consisting of just two functions. Note that the use of the terms \"front-end\" and \"back-end\" here is distinct from their use in connection with the timetable generation code, where \"back-end\" refers to the actual timetable generation function (currently only <code>FET</code> is supported).</p>","path":["Some details of the GUI code"],"tags":[]},{"location":"GUI_details.html#communicating-with-the-back-end","level":2,"title":"Communicating with the back-end","text":"<p><code>backend.cpp</code> and <code>backend.h</code> contain the code for handling <code>libfetrunner</code>.</p> <p>The function <code>Backend::op</code> builds a single string by joining the actual command string with the arguments to the call using \"|\" as separator. This is used as the argument to the library function <code>void FetRunnerCommand(char *)</code>. Then it reads all logged lines, one by one, using <code>Backend::readlogline</code>, until one of the terminating lines is read.</p> <p><code>Backend::op</code> returns a list of \"result\" lines, parsed into key/value pairs. Also the lines reporting errors are collected (up to a limit) and passed by the <code>Backend::error</code> signal to the GUI, which can then show a pop-up message.</p> <p><code>Backend::readlogline</code> reads a single log entry using the other library function, <code>char* FetRunnerReadLog()</code>. Apart from return this entry as a key/value pair, it passes the entry to the log display widget, determining the colour according to the values in the key lookup table, <code>colours</code> (in <code>backend.cpp</code>).</p> <p>Note that there are two types of key/value pair here! Initially, a log entry is split at the first space character to provide key and value. If the entry consists of only a single word, that will be the key, the value will be empty. In <code>Backend::op</code> the \"result\" entries (\"$ key=value\") have their value (\"key=value\") split at the \"=\" character to provide the result key/value pairs which are returned from the call to <code>Backend::op</code>.</p>","path":["Some details of the GUI code"],"tags":[]},{"location":"GUI_details.html#dynamic-updating-during-a-run","level":2,"title":"Dynamic updating during a run","text":"<p>Because it waits for log entries before returning, <code>Backend::op</code> it is only suitable for commands which will be executed quickly (otherwise the GUI would hang). This works for most of the available commands, but the command \"RUN_TT\", which starts the actual <code>fetrunner</code> run, takes a long time. When a command is really finished and it will generate no further log entries, \"---\" is logged. But there is also the log entry \"*\" which signals the beginning of the long-running part. \"<code>Backend::op</code>\" also returns when it reads this log entry. Subsequent entries are then handled in a separate thread.</p> <p>In <code>threadrun.cpp</code> and <code>threadrun.h</code> there is a worker thread, <code>RunThreadWorker::ttrun()</code> which reads from the log using <code>Backend::readlogline</code> until it reads the completion signal \"---\". It acts on the various log entries indicating progress by emitting corresponding signals. These signals are forwarded to signals of the <code>RunThreadController</code>, which encapsulates the handling of the worker thread, isolating it from the main code. These signals are thus connected indirectly to methods (slots) of <code>FetRunner</code> with the same name which update the corresponding display elements.</p> <ul> <li> <p>\".TICK\" -&gt; <code>FetRunner::ticker()</code></p> </li> <li> <p>\".NCONSTRAINTS\" -&gt; <code>FetRunner::nconstraints()</code></p> </li> <li> <p>\".PROGRESS\" -&gt; <code>FetRunner::iprogress()</code></p> </li> <li> <p>\".START\" -&gt; <code>FetRunner::istart()</code></p> </li> <li> <p>\".END\" -&gt; <code>FetRunner::iend()</code></p> </li> <li> <p>\".ACCEPT\" -&gt; <code>FetRunner::iaccept()</code></p> </li> <li> <p>\".ELIMINATE\" -&gt; <code>FetRunner::ieliminate()</code></p> </li> </ul>","path":["Some details of the GUI code"],"tags":[]},{"location":"Result_JSON.html","level":1,"title":"Structure of the JSON result","text":"<p>The JSON object presents information about the last successful \"base\" instance: which constraints were disabled and the placement of the activities. For individual constraints which led to <code>FET</code> errors, the <code>FET</code> message is recorded. The information is presented in terms of item indexes. That is, each presented item (day, hour, activity, constraint, room, ...) is referred to by an index (starting at 0). In order to correlate these items with those in the data source, there is a list of references for each of these item types. Each of these referenced objects has an \"Id\" field, which is a reference to the corresponding source item. Except for the constraint objects, they also have a \"Tag\" field, giving a short \"name\" for the item, which may be of relevance to the back-end.</p> <p>The items in the constraint list have additional fields:</p> <ul> <li>\"CType\" (constraint name), as used in the source, so it may differ from the <code>FET</code> constraint name</li> <li>\"Data\" depends on constraint type, it can be a simple value or a key/value map</li> <li>\"Weight\", an integer in the range 0 (ineffective) to 100 (\"hard\") – with <code>FET</code> source a non-linear conversion function is used</li> </ul> <p>Not every source constraint need have its own reference, some may be attached to a teacher or class, for example. In this case the item referred to would be passed in the Data field, the source-reference being empty. A constraint on a group of activities might place the indexes of these activities as a comma-separated list in the Data field.</p> <p>Bear in mind that the actual constraints used in the generation are back-end constraints (i.e. <code>FET</code> constraints), even if their \"CType\" fields refer to the source. There may not be an easy 1:1 correspondence in some cases. Multiple <code>FET</code> constraints might derive from a single source constraint. The reverse case is currently not supported.</p>","path":["Structure of the JSON result"],"tags":[]},{"location":"Result_JSON.html#reporting-impossible-constraints","level":3,"title":"Reporting \"impossible\" constraints","text":"<p>If any individual constraint was found to cause a FET error report, these reports are recorded in the \"ConstraintErrors\" map, the key being the constraint index, the value the message. TODO: Should \"timed out\" constraints also be reported here?</p>","path":["Structure of the JSON result"],"tags":[]},{"location":"automatic_constraint_switching.html","level":1,"title":"Automation of Constraint Testing","text":"<p>If the timetable generation process can't even start, there will be an error message which should help find the problem. Sometimes the process will start, but get stuck at a very early stage. In these cases it may not be too difficult to find the problem, but some assistance may be useful. However, it can happen that the computation time is uncomfortably long (whatever that means ...) and the user is uncertain as to whether it will ever complete. Although this is perfectly normal for more complicated timetables, it can make it difficult to trace problem areas. How long should one wait before deciding the timetable generation is not going to work? And then, what steps need to be taken to attain a timetable that might at least be a useful starting point?</p>","path":["Automation of Constraint Testing"],"tags":[]},{"location":"automatic_constraint_switching.html#the-aims","level":2,"title":"The aims","text":"<ul> <li> <p>To deliver some sort of \"complete\" timetable in a limited time, if necessary by disabling some of the constraints.</p> </li> <li> <p>To identify constraints which are difficult (slowing the timetable generation significantly) or impossible to satisfy. Note that it might be difficult to distinguish between these two cases.</p> </li> <li> <p>To permit, on demand, indefinite generation times, with the possibility of manual interruption. Gradually more and more of the constraints would be enabled and the latest \"successful run\" would then be available.</p> </li> </ul>","path":["Automation of Constraint Testing"],"tags":[]},{"location":"automatic_constraint_switching.html#automation","level":2,"title":"Automation","text":"<p>A common approach to troubleshooting a timetable is to disable and re-enable groups of constraints, aiming to find those constraints that are difficult (or impossible) to satisfy by narrowing down the areas in which they can lie.</p> <p>With few constraints the generation of solutions will often be very quick, allowing many tests to be carried out in a short time. Automation can be very helpful at this stage. It won't be able to replace the insights of expert timetable constructors, but can speed up the process even for them. As the run times get longer the advantages may be less obvious, but some automation of the process can still assist less experienced timetable constructors. The possibility of running tests in parallel when multiple processor cores are available can also offer speed gains.</p> <p>Automation along these lines should be seen as a valuable assistant, but not as a panacea. In general, it can't tell you exactly what needs changing, it can only point to areas where changes might be necessary. Often there will be several – perhaps seemingly unrelated – areas in which changes in the data (constraints) could lead to more computable timetables. Of course, although such a technique can be helpful, experience and analytical expertise are still very desirable qualities in a person charged with the task of constructing a timetable.</p>","path":["Automation of Constraint Testing"],"tags":[]},{"location":"automatic_constraint_switching.html#the-algorithm-used-here","level":2,"title":"The algorithm used here","text":"<p>The constraints are divided into \"hard\" ones – which must be satisfied – and \"soft\" ones – which are more or less desirable (a weighting is specified), but whose non-fulfilment would not necessarily prevent the generation of an acceptable timetable. There is also a separate, small category of very important hard constraints, which are processed before all the others.</p> <p>In order to get as many constraints as possible enabled, an attempt is made to begin with the \"easy\" ones. Note that it may not be immediately clear which constraints are \"easy\"! I don't know whether this is really the best approach, but it seems reasonable. Once more \"difficult\" constraints are included, the run times will be longer, so fewer tests can be carried out in a given time.</p> <p>The constraints are further divided into \"types\" (such as minimum-days-between-lessons or maximum-gaps-per-day). Some of these types have been given priorities, so that they are tested earlier or later in the process. Again, I don't know if this really helps – perhaps in some cases it does, in others not. Also, whether a particular constraint type can be generally regarded as more difficult than another is not clear. Constraints without a priority get the value 0. Positive priorities are handled earlier, negative ones later.</p>","path":["Automation of Constraint Testing"],"tags":[]},{"location":"automatic_constraint_switching.html#multiprocessing","level":3,"title":"Multiprocessing","text":"<p>It is assumed that multiple processor cores are available. If not, this algorithm will probably not be very helpful. A minimum of four is suggested. The program will still run if there are fewer processor cores, but up to four processes will be started anyway, so the whole thing will run slower and the timeouts will perhaps be too short.</p> <p>Also if too many processes are started, efficiency is likely to suffer (more runs are started, only to be terminated after a while without actually contributing anything). This has not been extensively tested, but setting a maximum of about six (when the cores are available) seems to be a good compromise.</p> <p>The limiting of the number of running processes is achieved by using a list of active processes. New runs may be queued at any time, but they will only be actually started if there are \"free\" processors.</p>","path":["Automation of Constraint Testing"],"tags":[]},{"location":"automatic_constraint_switching.html#monitoring-and-steering-the-processes","level":3,"title":"Monitoring and steering the processes","text":"<p>Overall control of the processes lies in a \"tick\" loop. Timeouts are managed and the progress (percentage of placed activities) of each run is monitored. Every second, the state of each run is read and necessary actions are taken, such as starting new runs from the queue or switching to the next phase (see below).</p> <p>There is a timeout for the whole program (as well as for individual timetable generation runs). When this is reached, all runs will be stopped and the latest successful run taken as the result. It is also possible to start the program without a timeout, so that it could – in principle – run indefinitely. The program can then be terminated externally. At least on Linux (other operating systems haven't been tested), using the \"terminate\" signal (Ctrl-C on the command line), the latest successful run should then be available as a result.</p>","path":["Automation of Constraint Testing"],"tags":[]},{"location":"automatic_constraint_switching.html#phase-0","level":3,"title":"Phase 0","text":"<p>Initially, three or four runs are started:</p> <ul> <li> <p>the unmodified, fully constrained data set,</p> </li> <li> <p>the same, but with all soft constraints (if any) disabled,</p> </li> <li> <p>a version with all constraints disabled, which will check that it is at all possible to place the activities within the time slots,</p> </li> <li> <p>a version with just a small set of \"very important\" hard constraints (which only runs if there are any such constraints).</p> </li> </ul> <p>Whichever phase is currently active, the successful completion of the fully constrained run will lead to the ending of the whole process.</p> <p>Phase 0 ends when the \"very important\" constraints have been processed, ideally with all being included in a successful run. At least the unconstrained run must complete successfully. If the very important (\"prioritised\") version completes successfully, the phase can be ended immediately. The result of this phase will be used as the basis for phase 1.</p> <p>If the second run (with all hard, but no soft constraints) completes successfully, phase 1 will be skipped or aborted (if it has already started), and this run will be used as the basis for phase 2.</p>","path":["Automation of Constraint Testing"],"tags":[]},{"location":"automatic_constraint_switching.html#phase-1","level":3,"title":"Phase 1","text":"<p>In this phase, hard constraints are added type-by-type. Within this phase (as also in phase 2) there are \"cycles\". In a cycle, all currently inactive constraints are collected and a run is initiated (queued) for each of the types. Each of these runs is an attempt to add a number of constraints of a single type to the current (known working) base. Each of these runs has a timeout, which is initially very short (a few seconds).</p> <p>When a run completes successfully, all other runs within this cycle are terminated and the successful run is taken as the basis for the next cycle.</p> <p>If a run fails or is timed out, it is added to the back of the queue, unless it has only one new constraint, in which case it is discarded. When this instance is later taken from the queue, it will be split into two new instances (each covering one half of the initial new constraints), the old instance then being discarded.</p> <p>The handling of \"timeouts\" is somewhat flexible. In essence there needs to be an algorithm for determining whether an instance is likely to complete within a given period of time. If a run is progressing \"too slowly\", or seems to be stuck, it can be terminated and added to the back of the queue for later splitting.</p> <p>Phase 1 ends when there are no unsatisfied hard constraints which haven't been discarded because of error or timeout. It is quite possible that this never occurs (at least, not before the program timeout), so phase 2 may never be started.</p>","path":["Automation of Constraint Testing"],"tags":[]},{"location":"automatic_constraint_switching.html#phase-2","level":3,"title":"Phase 2","text":"<p>This is basically the same as phase 1, but it deals with the soft constraints (all hard constraints now being enabled).</p>","path":["Automation of Constraint Testing"],"tags":[]},{"location":"automatic_constraint_switching.html#possible-development-areas","level":2,"title":"Possible development areas.","text":"<p>If the unconstrained instance fails, at present the error report comes from the back-end, which may be a bit cryptic and general. Further steps could be taken to trace difficulties within the activity collection, perhaps identifying \"difficult\" classes or teachers.</p> <p>At present the soft constraints are treated basically the same as the hard ones (they are just introduced later). If there are difficult hard constraints, the soft constraints may not play a role – they are only added when all the hard constraints have been shown to be satisfiable. But if the difficulties arise in connection with the soft constraints, there may be better ways of handling them. The current approach may produce reasonable results, but no alternatives have been tested yet.</p>","path":["Automation of Constraint Testing"],"tags":[]},{"location":"fetrunner.html","level":1,"title":"fetrunner","text":"<pre><code>– an assistant for the generation of school timetables\n</code></pre>","path":["fetrunner"],"tags":[]},{"location":"fetrunner.html#history","level":2,"title":"History","text":"<p><code>fetrunner</code> stems from a series of converter tools allowing certain commercial timetable programs to use <code>FET</code> for automatic timetable generation. This was desirable because of limitations of the commercial programs' internal generators.</p> <p>This worked pretty well. However, after several years of practical experience (generating timetables for a school), it became clear that quite a lot of insight into the generation process was necessary to get the best out of <code>FET</code>. That meant many runs with tweaked conditions (constraints) until a usable timetable was produced. It was sometimes quite an effort to trace the conditions which prevented the successful generation of a timetable meeting the specifications.</p> <p>The question arose as to whether this \"debugging\" process could be automated to some extent. It seemed likely as the steps one would typically perform were usually very similar from one case to another, involving systematic disabling and re-enabling of various groups of constraints. <code>fetrunner</code> is the result of efforts to answer this question.</p>","path":["fetrunner"],"tags":[]},{"location":"fetrunner.html#the-problem-to-solve","level":2,"title":"The problem to solve","text":"<p>When trying to produce a timetable with <code>FET</code> it can happen that the generation step doesn't run smoothly. In some cases it won't even start, returning an error message which may help in finding the unsatisfiable condition in the configuration file (\"xxx.fet\"), though sometimes the message may seem a bit cryptic. In other cases the process will start, but get stuck at an early stage. In other cases the progress will be very slow, so that it is not clear when (or if!) the process will be completed.</p> <p><code>fetrunner</code> is an attempt to assist in finding the conditions which lead to these problems. It does this by repeatedly running the command-line version of <code>FET</code>, each time with a different subset of the constraints specified in the configuration file.</p> <p>Somewhat as a side-product <code>fetrunner</code> also produces a fully populated timetable from its last successful configuration. That means that probably not all the constraints are satisfied, so it might not be all that useful, but under certain circumstances it might be helpful. Only if even the unconstrained configuration fails to run will there be no such placement data.</p>","path":["fetrunner"],"tags":[]},{"location":"fetrunner.html#how-it-works","level":2,"title":"How it works","text":"<p>Basically, it starts by deactivating all the constraints. If even that doesn't help, there may be an error message from <code>FET</code> which can offer some useful information.</p> <p>Then it activates a single type of constraint, which means all the individual constraints of a particular type, and starts a <code>FET</code> generation run. This is repeated, in principle, for all the constraint types used in the source file. Several of these processes can be run simultaneously (see Parallel processing).</p> <p>When one of these generation runs completes successfully, all the others are stopped and discarded. This first successful run is now taken as a new basis. For each of the remaining constraint types a new generation run is started, with the new constraint type being added to the successfully completed one. Again, the first successfully completing run is taken as a new basis, and so on until all constraint types have been enabled.</p> <p>Well, given an \"easy\" configuration file that might work within a reasonable time. But there would not be much point, because we might as well just run the original configuration file with all constraints enabled, which would be a lot simpler and quicker. <code>fetrunner</code> becomes interesting when one or more constraints are causing errors or holding up the generation run in some other way.</p> <p>Normally the first stages of the algorithm (the addition of the \"easiest\" constraint types) will run quickly, within a few seconds, allowing most of the constraints to be enabled in a fairly short time. But the longer the run times, the more difficult it is to cover all the remaining constraint types in a given overall run time – some \"easy\" constraints might never get tested.</p> <p>There are essentially two cases that need dealing with. Perhaps the simplest is when a constraint blocks the generation completely, in such a way that <code>FET</code> returns an error message. The other is when the generation run gets stuck or progresses only very slowly (it is not always easy to distinguish between these two). At first, while there are still constraint types which can be added whose runs complete quickly, non-completing runs are no problem because they will simply be discarded. But at some point, only the difficult constraint types remain.</p> <p>One of the aims of <code>fetrunner</code> is to produce a timetable within a limited, specified time. When the configuration is \"challenging\", this may only be possible by leaving some of the constraints disabled, even though they might be satisfiable (given enough time).</p>","path":["fetrunner"],"tags":[]},{"location":"fetrunner.html#hard-and-soft-constraints","level":3,"title":"Hard and soft constraints","text":"<p>A constraint is categorised as \"hard\" if it must be satisfied in an acceptable timetable. <code>fetrunner</code> loosens this requirement a bit in order to get all the activities placed within a given time. The resulting timetable may not be acceptable yet, but perhaps it can be of help. A \"soft\" constraint is one which should be satisfied \"if possible\", but a resulting timetable may be acceptable even if the constraint is not satisfied. In <code>FET</code>, hard constraints have \"weight\" 100 (%) – for any smaller value the constraint is considered soft (even if, say, 99.99% is not particularly soft).</p> <p><code>fetrunner</code> takes the distinction between hard and soft constraints into account in that it doesn't touch the soft constraints (it leaves them deactivated) until all the hard constraints have been satisfied (or rejected as \"impossible\"). Thus, it is possible that the soft constraints never get tested, if the processing of the hard constraints takes up all the available time. There is a further (fixed) group of \"prioritised\" hard constraint types, which are tested in the initial phase, before the main group of hard constraints. These are the constraints detailing concrete blockages in the availability of classes, teachers and rooms, as well as fixed activity placements.</p> <p>Because of the way <code>fetrunner</code> works, it doesn't make much sense to actually use soft constraints in their original form. In <code>FET</code> a soft constraint is rejected if a number (dependent on the weight) of placement attempts conflict with it. <code>fetrunner</code> does something vaguely similar, in that it tries to use the constraint, only accepting it if a run completes successfully. In <code>fetrunner</code>, the weight of the soft constraints is used to determine the initial order in which the constraints are enabled for testing, so that ones with a higher weight are introduced first. The constraint itself is run in <code>FET</code> as a hard constraint (weight 100). As this approach has not yet been widely tested, there is a flag which can be set to cause the original weight to be used, so that the effectiveness can be compared.</p>","path":["fetrunner"],"tags":[]},{"location":"fetrunner.html#handling-difficult-constraints","level":3,"title":"Handling \"difficult\" constraints","text":"<p>Many constraints will be satisfied easily, and <code>fetrunner</code> tries to enable these first, so that as many constraints as possible get enabled in the given time. As soon as time-consuming (\"difficult\") constraints are enabled, each individual test run takes longer, so fewer tests are possible. Some constraint types may be more prone to being \"difficult\" than others, but in general this depends on the input data.</p> <p>The algorithm uses various tests to determine whether a set of constraints (of one type) is \"difficult\". These have been developed more or less by trial and error, and may not be at all optimal, if indeed there is such a thing in this situation. The primary considerations are whether a run is likely to complete, and how long it will take. A <code>FET</code> run produces a completion measure – how many activities have been placed. In combination with the elapsed time, <code>fetrunner</code> uses this to calculate a rate of progress.</p> <p>If the rate of progress is too low, the run may be terminated and the list of constraints split into two halves, which are then (later, when processing capacity becomes available) run separately. In this way a binary search procedure for potentially blocking constraints is implemented. If there is such a blocking constraint, this process will lead eventually to a run in which this is the single \"added\" constraint. If this run is deemed to be \"stuck\", the constraint is marked as \"impossible\" and removed from consideration.</p> <p>In some cases a constraint really will be impossible, at least in combination with others, and there may be a more or less helpful message from <code>FET</code>. In other cases it may just be that more time would be needed to incorporate this constraint. It is in the nature of <code>fetrunner</code> that it cannot always distinguish between the two (because of the general complexity of the timetabling problem).</p> <p>The acceptable rate of progress varies according to the overall state of the process. At the beginning only rapidly progressing runs will be accepted, so that as many \"easy\" constraints as possible get accepted. As more constraints are enable, the run time will generally increase, so lower rates of progress must be tolerated. The acceptable rate is made dependent on the completion time for the last successful run.</p>","path":["fetrunner"],"tags":[]},{"location":"fetrunner.html#parallel-processing","level":3,"title":"Parallel processing","text":"<p>The number of timetable-generation runs which are started at any time depends on the number of processor cores available. Experiments indicate, however, that there may be something like an optimal number, too many processes perhaps even reducing the overall performance. To keep the main part of the algorithm separate from the handling of parallel processes, a process queue is used. Any number of generation runs may be added to the queue, but a run will only be actually started if one of the limited number of CPU processes is available.</p> <p>As the algorithm depends on multiple processes running simultaneously, the effectiveness depends on there being enough processor cores available. A minimum of four parallel processes is assumed, so if fewer cores than this are available, performance will suffer greatly. A few experiments indicated that around six parallel processes can offer a significant improvement, but that increasing the number further may not be helpful.</p> <p>It is not easy to determine what the optimum number of parallel processes is, indeed it probably depends on the data fed in. Many runs are simply discarded along the way, so in a sense it is rather wasteful of computer resources. Also, the more parallel runs there are, the more are discarded. This may be the main factor leading to loss of performance when larger numbers of processes are run in parallel, perhaps connected with the memory accesses involved – although the multiple cores may be (more or less) independent of each other, they all access the same memory.</p> <p>At present the program allows at least four parallel processes, even if fewer real processor cores are available, and a maximum of up to six, depending on the number of available cores. Thus a CPU with at least six cores is recommended.</p>","path":["fetrunner"],"tags":[]},{"location":"fetrunner.html#special-generation-runs","level":3,"title":"\"Special\" generation runs","text":"<p>In addition to the timetable-generation runs described above, which test individual constraint types, there are four additional ones. One configuration is run in which all constraints are disabled. Only when this completes successfully is the main part of the algorithm (as described above) started. If this doesn't complete successfully, there is no point in trying to add constraints. Should this happen there will often be an error report from <code>FET</code>, which is recorded in the log. No result can be generated in this case, because no run completed successfully.</p> <p>Then there is a fully constrained run. This covers cases in which the original data can be processed successfully within the given time, making the rest of the algorithm redundant.</p> <p>There is also a run with all the hard constraints and no soft constraints enabled. If there are no soft constraints anyway, it will just be a second fully constrained run. Otherwise, it may complete before the step-wise accumulation of the constraint types finishes (with the hard constraints), allowing that part of the algorithm to be aborted, jumping straight to the start of the soft-constraint accumulation. This can be useful in cases where the hard constraints can all be satisfied within a convenient time, allowing soft-constraint testing to start earlier.</p> <p>A fourth process gathers a group of prioritised hard constraints – not-available times for classes, teachers and rooms, and fixed activity placements. This process will only be run of there are any such constraints. Addition of further constraints is blocked until either this process is complete or as many as possible of the constraints concerned have been incorporated into the base set. See \"Phases\" (TODO) for further details.</p>","path":["fetrunner"],"tags":[]},{"location":"fetrunner.html#progress-monitoring-and-completion","level":2,"title":"Progress monitoring and completion","text":"<p>Apart from the timeouts of the individual runs, there is also an overall timeout, which determines the maximum length of a <code>fetrunner</code> run. Around five minutes seems reasonable in many cases and this is set as the default. But there are situations in which a shorter or longer run may be desirable, so it is possible to specify any number of seconds. If the timeout is set to 0, there is no timeout, so the run could possibly go on indefinitely. In such a case it may be necessary to terminate the run manually.</p> <p>When the algorithm finishes, the results are available as a JSON object detailing the constraints which were deactivated and the activity placements resulting from the last successful run (the latest \"basis\").</p> <p>While the algorithm is running it produces a log, documenting the progress. This can be useful for diagnosing program errors, but may also be used for run-time feedback of the progress.</p>","path":["fetrunner"],"tags":[]},{"location":"how_fetrunner_works.html","level":1,"title":"How <code>fetrunner</code> works","text":"<p>The basic idea is to behave similarly to a person looking for possible problems in a <code>FET</code> file. The constraints are divided into types and hard/soft collections. By progressively enabling the groups of constraints belonging to each type (in a certain order, set in the program), the program attempts to include as many as possible of the specified constraints consistent with <code>FET</code> delivering a solution within a reasonable time.</p> <p>The program has strategies for deciding whether a run has got stuck or is progressing too slowly. When an instance fails or is determined to be too slow, it is aborted. If the list of new constraints it is testing has more than one entry, two new instances will be generated, each with half of the original constraints. In this way, individual constraints which are difficult can be isolated and eliminated. As more and more constraints are enabled, it is to be expected that the time to reach a solution will increase. The time-out strategies try to take this into account.</p> <p>Initially, three or four \"special\" instances are started:</p> <ul> <li>fully constrained</li> <li>all the hard constraints, but no soft constraints</li> <li>\"priority\" constraints only (hard, non-available time slots and fixed activity placements)</li> <li>unconstrained</li> </ul> <p>These instances are not \"splittable\". Under normal circumstances these instances run until they complete naturally. If there are no priority constraints, this instance will not be run. If there are no hard constraints, the second instance will be a duplicate of the first (but will still run).</p> <p>When a non-special instance completes successfully, all other current non-special instances are aborted. The successful instance is then used as a new basis for regenerating the next instances to be tested, one for each of the remaining constraint types relevant for this phase. The successful completion of a special instance is handled separately.</p> <p>The overall operation is divided into \"phases\". On entering the first phase (phase 0), these special instances are already running. Instances for each of the priority-constraint types are generated, so that testing starts with just these. New instances are added to a queue, so that they can be run when processor cores become available.</p> <p>Normally, the unconstrained instance will complete very quickly, so that there is at least a very minimal successful run available as a result. If even the unconstrained instance fails or is aborted (timed out), there will be no result at all.</p> <p>The first phase ends when either the priority-constraints instance completes successfully or when as many as possible of the priority constraints have been included in a successful <code>FET</code> run.</p> <p>In the second phase (phase 1), all the remaining hard constraints are tested. On entry, all running (and queued) instances except the specials \"fully constrained\" and \"all hard constraints\" will be cancelled. A new set of instances, one for each of the remaining hard constraint types, is generated based on the last successful instance from the first phase. These are added to the queue to be run when processor cores become available.</p> <p>The program tries to favour constraint types which are easily satisfied, by testing them earlier (it has an internal ordering for the various types). In this way it should be possible to include as many constraints as possible within a limited time. However, there is no sure way of knowing which types are going to be easier, so it's little more than guess-work.</p> <p>The second phase ends when either the \"all hard constraints\" instance completes successfully or when as many as possible of the hard constraints have been included in a successful <code>FET</code> run.</p> <p>In the third phase (phase 2), all the soft constraints are tested. On entry, all running (and queued) instances except the special \"fully constrained\" will be cancelled. A new set of instances, one for each of the soft constraint types, is generated based on the last successful instance from the second phase. These are added to the queue to be run when processor cores become available.</p> <p>The order in which the soft constraint types are added depends on their weight, those with the largest weight being tested first. It is worth noting that, by default, <code>fetrunner</code> runs the soft constraints in <code>FET</code> as if they were hard. This can be seen as an alternative way of dealing with soft constraints which is more in tune with the way <code>fetrunner</code> works. Indeed, even the hard constraints are treated by <code>fetrunner</code> as if they were soft, by eliminating them if they seem to be too \"difficult\". Thus the main difference between hard and soft constraints is that the soft constraints are only tested when testing of the hard constraints is finished.</p> <p>The third phase ends when either the \"fully constrained\" instance completes successfully or when as many as possible of the soft constraints have been included in a successful <code>FET</code> run. At this point the tidy-up phase (phase 3) is entered, which just waits for all remaining active (but now cancelled) instances to finish cleanly. Then the final result can be constructed.</p> <p>With each successful run contributing its set of constraints to the new runs, there is always a \"best so far\" <code>FET</code> instance, which gradually gathers more and more constraints (ideally!).</p> <p>When the time limit for the program is reached, or when it is manually interrupted, the latest successful run is taken as the final result.</p> <p>There are some difficult cases with which <code>fetrunner</code> can't help much, because the basic runs take too long, but for many <code>FET</code> files it can provide some useful information.</p>","path":["How fetrunner works"],"tags":[]},{"location":"modular_program_structure.html","level":1,"title":"Modular program structure","text":"<p>The program is divided functionally into Go packages. On the whole these packages are not independent of each other and not designed to be used outside this program</p>","path":["Modular program structure"],"tags":[]},{"location":"modular_program_structure.html#package-base","level":2,"title":"Package <code>base</code>","text":"<p>The <code>base</code> package provides logging and reporting functions.</p> <p>Also implemented in this package is a simple database structure with information about the institution – at present focusing primarily on the data connected with timetabling, but this can be easily extended. The root of this data structure is the <code>Go</code> struct <code>DbTopLevel</code>.</p> <p>The database structure can be loaded from and saved to JSON, but other storage forms should be fairly straightforward to implement. There are functions to assist in conversion from other structures. Only a limited set of constraints is supported at present, which is intentional – the aim is to keep it as simple as possible, whilst supporting enough constraints to make it useful. If necessary, it should be easy enough to add new constraints.</p> <p>The  <code>Go</code> struct <code>BaseData</code> exists to tie together all the information and data structures associated with a particular data set. This avoids global variables and allows, potentially, multiple data sets to be handled at the same time.</p>","path":["Modular program structure"],"tags":[]},{"location":"modular_program_structure.html#package-timetable","level":2,"title":"Package <code>timetable</code>","text":"<p>To prepare for building a timetable the data in the <code>base</code> package is preprocessed somewhat to populate the <code>Go</code> struct <code>TtData</code>, which is used as the data source, to be converted to the format needed by the back-end. This structure must be rebuilt if the underlying data (in <code>DbTopLevel</code>) is changed. This timetable \"preparation\" is separated out into the package <code>timetable</code>.</p>","path":["Modular program structure"],"tags":[]},{"location":"modular_program_structure.html#package-autotimetable","level":2,"title":"Package <code>autotimetable</code>","text":"<p>This package provides the main algorithm for systematically testing the timetable generation with different combinations of constraints. It is initially designed around the <code>FET</code> timetable generator, but should not be too tightly tied to it, so that it could, in principle, be used with a different timetable-generator back-end. It communicates with the back-end via an interface to maintain this independence.</p> <p>The algorithm runs multiple versions of the data, each with a different subset of the constraints enabled, in an attempt to determine any constraints which may be difficult or impossible to fulfil. It also tries to deliver a timetable – possibly not fulfilling all the constraints – within a set time.</p> <p>It is designed around the idea of a \"data source\" and a \"generation back-end\", both of which can – in principle be implemented in various ways. The most straightforward is to use a <code>FET</code> file (\"xxx.fet\") as source and <code>FET</code> as the back-end. However, there is also support for a sort of JSON-like database structure as data source, which can be built or converted from the data structures of other timetable programs, if there is a suitable interface. The package \"w365\" supports the reading of custom data from the \"Waldorf 365\" school management software.</p> <p>For managing the data connected specifically with this package the <code>Go</code> struct <code>AutoTtData</code> is used, which can be filled from the data in <code>TtData</code> and <code>DbTopLevel</code>.</p> <p>For <code>FET</code> input files (\".fet\" ending), there is – at present – no conversion from <code>FET</code> data to <code>DbTopLevel</code>, the data for the back-end is derived directly from the source in this case. The conversion to <code>DbTopLevel</code> might be possible, but would be difficult to generalise to all <code>FET</code> files, because of the rather different structures in <code>FET</code>.</p>","path":["Modular program structure"],"tags":[]},{"location":"modular_program_structure.html#package-fet","level":2,"title":"Package <code>fet</code>","text":"<p>This provides a back-end based on a <code>FET</code> file. It handles regeneration of the <code>FET</code> file with different constraints enabled, and starts and monitors the <code>FET</code> process. The <code>FET</code> configuration data is supplied in a particular structure, <code>Go</code> struct <code>fet_build</code>, which can be constructed from an actual <code>FET</code> input file, but can also be produced from some other data – in particular, constructing it from <code>TtData</code> structures is supported by the <code>BuildFet</code> function.</p> <p>To handle input from an actual <code>FET</code> file, there is a <code>FET</code>-file reader, which parses a <code>FET</code> input file to produce a <code>TtSourceFet</code> structure, from which the <code>PrepareFet</code> function can produce the <code>fet_build</code> for the <code>FET</code> back-end.</p>","path":["Modular program structure"],"tags":[]},{"location":"modular_program_structure.html#package-w365tt","level":2,"title":"Package <code>w365tt</code>","text":"<p>This package reads a JSON file generated by the <code>Waldorf 365</code> program into the database structure <code>DbTopLevel</code>. This allows the <code>fetrunner</code> algorithm to be used as a timetable generator for this program. Similar packages could be written to handle data from other software, if the requisite information is available (which it unfortunately often isn't ...).</p>","path":["Modular program structure"],"tags":[]},{"location":"temporary_files.html","level":1,"title":"Temporary files","text":"<p><code>fetrunner</code> starts many <code>FET</code> (<code>fet-cl</code>) instances, each of which produces a number of output files. Only a fraction of these are needed by <code>fetrunner</code>, and none of them are retained. To reduce wear on SSD storage, these should probably be stored in an in-memory file system (RAM-disk, etc.). Linux has such a file-system \"built-in\" (at <code>/dev/shm</code>), and <code>fetrunner</code> uses it for these temporary files. On other operating systems it may be possible to provide something like this, but perhaps only with third-party software.</p> <p>There are a number of utilities for Windows which can generate RAM-disks. Two free ones which seem to work are AIMtk and OSFMount. Of these OSFMount seems a bit easier to use, but AIMtk can produce a dynamic RAM-disk which only occupies as much memory as is needed – OSFMount allocates a fixed-size block of RAM. However, <code>fetrunner</code> would normally need relatively little space, and a few hundred megabytes should be more than enough. When <code>fetrunner</code> starts it looks for a drive mounted at \"R:\", so if possible a RAM-disk should be mounted here.</p> <p>If no such file-system is available and detected, the standard temporary directory for the operating system will be used. With the command-line version of <code>fetrunner</code>, it is possible to specify the path to the directory to be used for temporary files using the \"-tmp\" option. If a Windows system has a RAM-disk mounted at \"M:\", the option would then be <code>-tmp M:\\</code>. In the GUI version of <code>fetrunner</code> the path is shown and can be changed on the \"Settings\" tab.</p> <p>Within this temporary folder, each run of <code>fet-cl</code> gets its own sub-directory for its files. The name of this sub-directory is derived from the source file by removing the extension. If multiple instances of <code>fetrunner</code> are to be run simultaneously – which is generally inadvisable because of the limited processor cores – each must have a unique source file name.</p>","path":["Temporary files"],"tags":[]},{"location":"threads_processes.html","level":1,"title":"Threads and Processes","text":"<p><code>fetrunner</code> is at its very core multi-threaded. It runs several timetable generations in parallel, seeking those that complete most quickly. Each timetable generation has its own thread which runs <code>FET</code> (<code>fet-cl</code>) as a blocking sub-process. As not all of these sub-process will run to completion, it must be possible to cancel them from another thread. In Go, so-called \"goroutines\" are used to manage threads.</p>","path":["Threads and Processes"],"tags":[]},{"location":"threads_processes.html#running-fet-on-a-data-set","level":2,"title":"Running <code>FET</code> on a data set","text":"<p>The function <code>RunBackend</code> in <code>fet/run_fet.go</code> is called to execute <code>FET</code> on a set of data supplied as a <code>TtInstance</code> structure. It first constructs a <code>FET</code> file from the <code>TtInstance</code>, then, by using <code>exec.CommandContext</code> together with a <code>context.WithCancel</code> to run <code>FET</code> on this file, a function is made available which can be called to abort the <code>FET</code> process. The actual process is started in a separate goroutine, allowing <code>RunBackend</code> to return immediately. It sets a data structure (<code>FetTtData</code>) as <code>InstanceBackend</code> on the <code>TtInstance</code>, which allows the progress of the <code>FET</code> process to be monitored and controlled.</p>","path":["Threads and Processes"],"tags":[]},{"location":"using_fetrunner.html","level":1,"title":"Using <code>fetrunner</code>","text":"<p>The GUI (Using the GUI) allows selection of <code>FET</code> files and processing parameters in a fairly straightforward way. It also shows the progress of a run dynamically. In some cases, however, the command-line tool may be more convenient. To use this, see Running the command-line tool.</p>","path":["Using fetrunner"],"tags":[]},{"location":"using_fetrunner.html#how-to-understand-the-results-of-a-fetrunner-run","level":2,"title":"How to understand the results of a <code>fetrunner</code> run","text":"<p><code>fetrunner</code> produces at least two result files in the same folder as the source file: from \"xxx.fet\", these would be \"xxx_Result.fet\" and \"xxx_Result.json\". The command-line version also produces a log file, \"xxx.log\". In the GUI version, the current log information is displayed in one of the tabs (older log information is not retained).</p> <p>If you open the \"xxx_Result.fet\" file in <code>FET</code>, you can see that some of the constraints have been deactivated, e.g. in the \"Time\" (and perhaps \"Space\") tab select \"All\". These are the ones which <code>fetrunner</code> decided were \"difficult\" or impossible.</p> <p>At the end of the log, there should be a summary of the accepted constraints.</p> <p>The \"xxx_Result.json\" file may contain some information about why a constraint was rejected, but it is rather intended for reading by other software. Nevertheless, a web browser (e.g. <code>Firefox</code>) can display this file quite neatly.</p> <p>The fact that <code>fetrunner</code> has deactivated a constraint doesn't mean that the constraint is necessarily impossible, though it may be (at least, in combination with other constraints). Another run, perhaps with a different timeout, might give a different result. The results show constraints whose removal makes it easier for <code>FET</code> to generate a timetable. If these constraints are important, it may be necessary to change other constraints which somehow interact with the shown ones – finding these may not be easy ...</p> <p>Looking at a timetable generated (in <code>FET</code>) from the \"xxx_Result.fet\" file (which is now known to be possible!) and at the deactivated constraints will – I hope – help you to discover how you might need to modify your data (activities and constraints) in order to get an acceptable result.</p> <p>Actually, there is a generated timetable (from \"xxx_Result.fet\") in \"xxx_Result.json\", but I haven't written the software to display this (yet).</p>","path":["Using fetrunner"],"tags":[]},{"location":"using_fetrunner.html#running-the-command-line-tool","level":2,"title":"Running the command-line tool","text":"<p>Important: By default the <code>FET</code> command-line executable is expected to be in the same directory as the <code>fetrunner</code> executable, or else runnable by calling <code>fet-cl</code> (on Windows the executable is <code>fet-cl.exe</code>), i.e. in the user's <code>PATH</code>. There is, however, a command line option (\"-fet\") to specify a different location – the value must be a full, absolute path.</p> <p><code>fetrunner</code> can be run with just the source file as argument:</p> <pre><code>./fetrunner path/to/fetfile.fet\n</code></pre> <p>This will normally run for up to five minutes, placing the results in the same directory as the source file, \"path/to/\" in the case of the above command:</p> <pre><code>`fetfile_Result.fet` – the `FET` file used to produce the result\n\n`fetfile_Result.json` – the results of the run, including the placements of the activities and the constraints which were deactivated\n\n`fetfile.log` – a log file giving information about the run\n\n`_fetfile.fet` – (optional, primarily for test purposes, with -xf option) should be essentially the same as the original `fetfile.fet`, but the constraints are indexed (in their \"Comments\" field) and the soft constraints are made \"hard\" (a derived weight, not the same as the original `FET` \"Weight_Percentage\" is added to the \"Comments\" field)\n</code></pre> <p>The log-file is updated continually during the run, so it is possible to monitor progress by reading this file.</p> <p>A run can be stopped prematurely by pressing <code>Ctrl-C</code>. This will probably take a couple of seconds to work, as it tries to tidy up. The result files will be produced from the current state.</p> <p>There are a few command-line options:</p> <pre><code>fetrunner -help\n -&gt;\n  -fet string\n        FET executable: /path/to/fet-cl\n  -h    skip hard constraint testing phase\n  -p int\n        max. parallel processes\n  -s    the weights of soft constraints are retained\n  -t int\n        set timeout, s (default 300)\n  -tmp string\n        Folder for temporary files (FET): /path/to/tmp\n  -v    print version and exit\n  -xd\n        run in debug mode\n  -xf\n        write fully-constrained FET file\n  -xt\n        run in testing mode\n</code></pre> <p>If it is known that the hard constraints are all satisfiable, the \"-h\" option can be used to always include the hard constraints (the unconstrained instance is not run) and test the sequential addition of just the soft constraints.</p>","path":["Using fetrunner"],"tags":[]},{"location":"using_the_gui.html","level":1,"title":"Using the GUI","text":"<p>The <code>fetrunner</code> GUI offers a convenient way of using <code>fetrunner</code>, allowing files to be loaded, parameters to be set and runs to be initiated. When a <code>fetrunner</code> process is running, the GUI provides various views on to the state and progress of the run.</p> <p>To exit the program, click on the window-close button. If a <code>fetrunner</code> process is running, <code>fetrunner</code> will take a few seconds to tidy up before quitting completely.</p> <p></p>","path":["Using the GUI"],"tags":[]},{"location":"using_the_gui.html#the-display-areas","level":2,"title":"The display areas","text":"<p>At the top of the window the currently loaded source file is shown, split into file name and folder. There is also a button to load a different one.</p> <p>Below this, there are two panels:</p>","path":["Using the GUI"],"tags":[]},{"location":"using_the_gui.html#the-left-panel","level":3,"title":"The left panel","text":"<p>At the top a few of the most useful parameters are shown and can be changed.</p>","path":["Using the GUI"],"tags":[]},{"location":"using_the_gui.html#parameters","level":4,"title":"Parameters","text":"<ul> <li> <p>Time limit: When a run reaches this elapsed time, it will be aborted. The latest successful <code>FET</code> instance will be taken as the result.</p> </li> <li> <p>Test soft constraints: Ticking this box will include all hard constraints as the basis for every <code>FET</code> instance, the two special instances with no constraints and only hard constraints will not be run. It is intended for cases where the hard constraints are known to be OK and we want to concentrate on the soft constraints.</p> </li> <li> <p>\"Real\" soft constraints: Retain the original weights of the soft constraints. Otherwise they are made \"hard\" for the <code>FET</code> runs. </p> </li> <li> <p>Processes (max.): The maximum number of <code>FET</code> instances that may be run simultaneously. 6 seems to be a good number, if that number of processor cores are available. More might be wasteful of resources and not necessarily more effective. Fewer than 4 won't allow the algorithm to work properly. If the host computer doesn't have 4 cores, the number of processes should still be set to 4, but <code>fetrunner</code> will be much less effective.</p> </li> </ul> <p>The items below this only become relevant when a file has been loaded and a run started.</p> <p></p> <p>Below this the progress of the three \"special\" instances (<code>FET</code> processes) are shown. Note that if the \"Test soft constraints\" checkbox is ticked, only the \"Full\" instance is active.</p> <p>The next area, \"Enabled constraints\" shows the progress of the tests with single constraint types, that is, the increasing number of constraints which have been found to be fulfillable. It displays the same information as the panel at the top of the \"Run progress\" view, but in a different form.</p> <p>Then comes the \"Elapsed time\" counter, showing the number of seconds for which a run has been active.</p> <p>Finally there are the run controls. Here you can start, and also interrupt, a run. Stopping a run has the same effect as reaching the time limit: it will take a couple of seconds to become effective because it tidies up the running processes and collects the latest results.</p>","path":["Using the GUI"],"tags":[]},{"location":"using_the_gui.html#the-right-panel","level":3,"title":"The right panel","text":"<p>This contains several panels selected by tabs.</p>","path":["Using the GUI"],"tags":[]},{"location":"using_the_gui.html#tab-run-progress","level":4,"title":"Tab: Run progress","text":"<p>Here all the constraint types used in the source file are shown in a table. Above the table there are lines to display the total progress of all hard and all soft constraints (number of \"accepted\" constraints). In the table, the constraint types are placed in the last column because this is easier (and I think neater) to lay out.</p> <p>Note that for soft constraints, the constraint type is prefixed by \"nn:\", where \"nn\" is a number between \"00\" and \"99\" related to (but not the same as) the <code>FET</code> percentage weight.</p> <p>The numeric columns display (for the associated constraint type):</p> <pre><code>1) the number of \"accepted\" constraints;\n\n2) the total number of individual constraints;\n\n3) the elapsed time at which the number field was last changed.\n</code></pre> <p>When all the individual constraints of a type have been \"accepted\" (found to be fulfillable), the number is replaced by \"+++\".</p>","path":["Using the GUI"],"tags":[]},{"location":"using_the_gui.html#tab-run-state","level":4,"title":"Tab: Run state","text":"<p>On this page there are two tables. At the top, all the single-constraint-type <code>FET</code> instances are shown which completed successfully and which were \"accepted\", i.e. whose constraints have been added to the running collection.</p> <p>The numeric columns display (for the associated constraint type):</p> <pre><code>1) a simple line number, showing acceptance order;\n\n2) the number of individual constraints being tested in this instance;\n\n3) the total number of individual constraints;\n</code></pre> <p>This table also shows single constraints which have been \"rejected\" (because they may be impossible), as can be seen in the following screenshot:</p> <p></p> <p>Instead of the number of constraints, \"--- [n]\" is shown. The number in square brackets is the index given to the rejected constraint in the \"xxx_Result.fet\" file produced by this run. This should help to find exactly which constraint this line refers to. These entries also have a tool-tip, which can indicate why the constraint was rejected.</p> <p>At the bottom of the page, the currently running instances are shown together with their progress (in percent) (excluding the \"special\" instances). Many of these will be terminated before completion. Even if one reaches 100%, it will only be \"accepted\" (and this transferred to the upper table) if it is the first one in the current group to do so.</p> <p>The numeric columns display (for the associated constraint type):</p> <pre><code>1) the completion percentage of the `FET` process;\n\n2) the total number of individual constraints;\n\n3) the process run time at which the last progress change was recorded;\n\n4) the time-out for this process (note that this is handled very flexibly!).\n</code></pre> <p>Constraint groups with only one constraint are run without a time-out (shown as \"---\"), but they may still be terminated if they seem to be \"stuck\".</p>","path":["Using the GUI"],"tags":[]},{"location":"using_the_gui.html#tab-log","level":4,"title":"Tab: Log","text":"<p>This view is probably of little interest to the normal user. It shows the communication between the GUI and the back-end processor.</p>","path":["Using the GUI"],"tags":[]},{"location":"using_the_gui.html#tab-settings","level":4,"title":"Tab: Settings","text":"<p>This gives access to some settings which will not normally be needed, except perhaps for initial set-up.</p> <ul> <li> <p>Temporary files: <code>fetrunner</code> produces many temporary files, arising from the many calls to the <code>FET</code> command-line program. If possible, these will be saved in an in-memory file-system. See Temporary files for further details.</p> </li> <li> <p>Fet command: This shows the command used to run the <code>FET</code>command-line program. It may be necessary to change this occasionally (after an update, for example), or there may be multiple versions available on the machine. You can select the one used here. Clicking \"Default\" will try to restore the default value (in the same folder as the <code>`fetrunner</code> executable, or else in the systems standard \"PATH\").</p> </li> <li> <p>Fet version: The version of the selected <code>FET</code> program will be shown here.</p> </li> <li> <p>Generate fully constrained FET file. This causes a slightly modified version of the original <code>FET</code> file to be written to the folder containing the source file. It is given a \"_\" prefix and contains some additional information added by <code>fetrunner</code>. Unless the \"Real\" soft constraints option is set, any soft constraints will now have \"Weight_Percentage\" 100, i.e. have become \"hard\". The original weight is added to the \"Comments\" field, along with a constraint index.</p> </li> </ul> <p>Also the <code>fetrunner</code> version is shown.</p>","path":["Using the GUI"],"tags":[]},{"location":"W365/constraintelemente.html","level":1,"title":"Die Constraint-Elemente","text":"<p>Hier wird die Struktur der expliziten Constraint-Elemente in der w365.json-Datei beschrieben. Die Constraints, die sich auf die Ressourcen beziehen (Klassen, Lehrer, Räume), erscheinen in den Objekten dieser Ressourcen.</p> <p>Alle Felder sollten Werte haben, Voreinstellungen gibt es nur in Ausnahmefällen.</p> <p>Im Allgemeinen kann ein Kurs ein Course-Element oder ein SuperCourse-Element sein.</p>","path":["W365","Die Constraint-Elemente"],"tags":[]},{"location":"W365/constraintelemente.html#activitiesendday","level":2,"title":"ActivitiesEndDay","text":"<p>Die Activities des Kurses sollten am Ende des Schülertags liegen.</p> <pre><code>{\n    \"Constraint\" :  \"MARGIN_HOUR\",\n    \"Weight\" :      73,\n    \"Course\" :      \"2edfe663-c62b-4d05-ace2-0bedb0f4b672\"\n}\n</code></pre>","path":["W365","Die Constraint-Elemente"],"tags":[]},{"location":"W365/constraintelemente.html#afterhour","level":2,"title":"AfterHour","text":"<p>Die Activities der Kurse sollten nach der angegebenen Stunde – ausschließlich dieser Stunde – liegen.</p> <pre><code>{\n    \"Constraint\":   \"AFTER_HOUR\",\n    \"Weight\":       100,\n    \"Courses\":      [\n        \"2edfe663-c62b-4d05-ace2-0bedb0f4b672\"\n    ],\n    \"Hour\":         4\n}\n</code></pre>","path":["W365","Die Constraint-Elemente"],"tags":[]},{"location":"W365/constraintelemente.html#beforehour","level":2,"title":"BeforeHour","text":"<p>Die Activities der Kurse sollten vor der angegebenen Stunde – ausschließlich dieser Stunde – liegen.</p> <pre><code>{\n    \"Constraint\":   \"BEFORE_HOUR\",\n    \"Weight\":       100,\n    \"Courses\":      [\n        \"2edfe663-c62b-4d05-ace2-0bedb0f4b672\"\n    ],\n    \"Hour\":         4\n}\n</code></pre>","path":["W365","Die Constraint-Elemente"],"tags":[]},{"location":"W365/constraintelemente.html#automaticdifferentdays","level":2,"title":"AutomaticDifferentDays","text":"<p>Die Activities eines Kurses sollen an unterschiedlichen Tagen stattfinden. Mit „\"ConsecutiveIfSameDay\": true“ sollten sie – falls sie doch am selben Tag sind – direkt nacheinander sein.</p> <p>Dieser Constraint wird im Prinzip auf alle Kurse (mit zwei oder mehr Activities) angewendet. Wenn dieser Constraint nicht vorhanden ist, wird er mit \"Weight\": 100 angewendet.</p> <p>Einzelne Kurse können durch DaysBetween-Constraints anders geregelt werden.</p> <pre><code>{\n    \"Constraint\":           \"AUTOMATIC_DIFFERENT_DAYS\",\n    \"Weight\":               100,\n    \"ConsecutiveIfSameDay\": true\n}\n</code></pre>","path":["W365","Die Constraint-Elemente"],"tags":[]},{"location":"W365/constraintelemente.html#daysbetween","level":2,"title":"DaysBetween","text":"<p>Dieser Constraint ist wie AutomaticDifferentDays, erlaubt aber andere Tagesabstände als 1 und wird auf einzelne Kurse angewendet. Mit „\"DaysBetween\": 1“ wird der globale Constraint AutomaticDifferentDays für diese Kurse ausgesetzt.</p> <pre><code>{\n    \"Constraint\":           \"DAYS_BETWEEN\",\n    \"Weight\":               100,\n    \"Courses\":              [\n        \"2edfe663-c62b-4d05-ace2-0bedb0f4b672\"\n    ],\n    \"DaysBetween\":          2.\n    \"ConsecutiveIfSameDay\": true\n}\n</code></pre>","path":["W365","Die Constraint-Elemente"],"tags":[]},{"location":"W365/constraintelemente.html#daysbetweenjoin","level":2,"title":"DaysBetweenJoin","text":"<p>Anders als DaysBetween wird dieser Constraint zwischen den einzelnen Stunden zweier verschiedener Kurse angewendet, also Kurs 1, Activity1 : Kurs 2, Activity 1; Kurs 1, Activity 2 : Kurs 2, Activity 2; Kurs 1, Activity2 : Kurs 2, Activity 1; ...</p> <pre><code>{\n    \"Constraint\":           \"DAYS_BETWEEN_JOIN\",\n    \"Weight\":               100,\n    \"Course1\":              \"2edfe663-c62b-4d05-ace2-0bedb0f4b672\",\n    \"Course2\":              \"5fda67de-bbb3-48a2-a098-d957796b7743\",\n    \"DaysBetween\":          1,\n    \"ConsecutiveIfSameDay\": false\n}\n</code></pre>","path":["W365","Die Constraint-Elemente"],"tags":[]},{"location":"W365/constraintelemente.html#parallelcourses","level":2,"title":"ParallelCourses","text":"<p>Die Activities der Kurse sollen gleichzeitig stattfinden. Die Anzahl und Länge der Activities müssen in allen Kursen gleich sein.</p> <pre><code>{\n    \"Constraint\":           \"PARALLEL_COURSES\",\n    \"Weight\":               100,\n    \"Courses\":              [\n        \"2edfe663-c62b-4d05-ace2-0bedb0f4b672\",\n        \"5fda67de-bbb3-48a2-a098-d957796b7743\"\n    ],\n}\n</code></pre> <p>TODO: Folgende Constraints sind noch nicht klar. Es kann gut sein, dass sie auf bestimmte Klassen (...) begrenzt oder anders formuliert sein sollten.</p>","path":["W365","Die Constraint-Elemente"],"tags":[]},{"location":"W365/constraintelemente.html#doubleactivitynotoverbreaks","level":2,"title":"DoubleActivityNotOverBreaks","text":"<p>Dieser Constraint sollte höchstens einmal vorkommen. Eine Doppelstunde soll nicht durch eine Pause unterbrochen werden. Die Pausen sind unmittelbar vor den angegebenen Stunden.</p> <pre><code>{\n    \"Constraint\":           \"DOUBLE_LESSON_NOT_OVER_BREAKS\",\n    \"Weight\":               90,\n    \"Hours\":                [2, 4]\n}\n</code></pre>","path":["W365","Die Constraint-Elemente"],"tags":[]},{"location":"W365/constraintelemente.html#minhoursfollowing","level":2,"title":"MinHoursFollowing","text":"<p>Zwischen den Stunden des ersten Kurses und denen des zweiten Kurses sollten (an einem Tag) mindestens die angegebene Zahl an Stunden liegen (die Stunden der zwei Kurse werden nicht mitgezählt).</p> <pre><code>{\n    \"Constraint\":   \"MIN_HOURS_FOLLOWING\",\n    \"Weight\":       90,\n    \"Course1\":      \"2edfe663-c62b-4d05-ace2-0bedb0f4b672\",\n    \"Course2\":      \"5fda67de-bbb3-48a2-a098-d957796b7743\",\n    \"Hours\":        4\n}\n</code></pre>","path":["W365","Die Constraint-Elemente"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html","level":1,"title":"Waldorf 365: Schnittstelle für die Stundenplanung","text":"","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#ausgabeformat","level":2,"title":"Ausgabeformat","text":"<p>Die Daten werden als JSON-Objekt ausgegeben, der Dateiname sollte mit \"_w365.json\" enden.</p>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#top-level-objekt","level":3,"title":"Top-Level-Objekt","text":"<pre><code>{\n    \"W365TT\": {},\n    \"FetData\": {},\n    \"Days\": [],\n    \"Hours\": [],\n    \"Teachers\": [],\n    \"Subjects\": [],\n    \"Rooms\": [],\n    \"RoomGroups\": [],\n    \"Classes\": [],\n    \"Groups\": [],\n    \"Courses\": [],\n    \"SuperCourses\": [],\n    \"Lessons\": [],\n    \"Constraints\": {}\n}\n</code></pre> <p>Die Array-Werte enthalten die ggf. geordneten Elemente des entsprechenden Typs. Alle Elemente sind JSON-Objekte. Diese Elemente haben ein optionales „Type“-Feld, das den Namen des Elements (\"Day\", \"Hour\", usw.) enthält.</p> <p>Einige Element-Namen sind anders als die entsprechenden Waldorf-365-Elemente:</p> <ul> <li>„TimedObject“ -&gt; „Hour“</li> <li>„Grade“ -&gt; „Class“</li> <li>„GradePartiton“ [sic!] -&gt; „Division“ (im Top-Level-Objekt nicht vorhanden, da kein Top-Level-Element)</li> <li>„EpochPlanCourse“ -&gt; „SuperCourse“</li> </ul> <p>Neu sind „W365TT“, „FetData“, „RoomGroup“ und „Constraint“. Es gibt auch  „SubCourse“ – einen Epochenkurs –, das als Unterelement von \"SuperCourse\" auftaucht.</p>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#w365tt","level":4,"title":"W365TT","text":"<p>In diesem Objekt könnten allgemeine Informationen oder Eigenschaften, die nirgendwo anders richtig passen, erscheinen, z.B.:</p> <pre><code>  \"W365TT\": {\n    \"Institution\": \"Musterschule Mulmingen\",\n    \"Scenario\": \"96138a85-d78f-4bd0-a5a7-bc8debe29320\",\n    \"FirstAfternoonHour\":   6,\n    \"MiddayBreak\":          [5, 6, 7]\n  },\n</code></pre> <p>\"FirstAfternoonHour\" und \"MiddayBreak\" sind für Constraints notwendig, die Nachmittagsstunden und Mittagspausen regeln.</p>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#fetdata","level":4,"title":"FetData","text":"<p>Ein optionales Objekt, das für die Übergabe von Programmparametern zur Verfügung steht. Aktuell sind keine Inhalte definiert.</p>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#constraint","level":4,"title":"Constraint","text":"<p>Diese Objekte haben verschiedene Felder, aber alle haben:</p> <pre><code>    \"Constraint\": string (Constraint-Typ)\n    \"Weight\": int (0 – 100)\n</code></pre> <p>Für diese Objekte gibt es eine eigene Dokumentation: Die Constraint-Elemente.</p>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#die-top-level-elemente","level":3,"title":"Die Top-Level-Elemente","text":"<p>Ich habe in jedem Element ein „Type“-Eigenschaft, das den Typ des Elements angibt. Da die Elementtypen über die Datenstruktur erkennbar sind, sind diese Felder nicht wirklich notwendig und könnten weggelassen werden. Vielleicht helfen sie aber, die JSON-Dateien etwas übersichtlicher zu machen. </p>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#day","level":4,"title":"Day","text":"<pre><code>{\n    \"Id\":       \"069240ee-b709-4fe2-813a-f04ce8c3614e\",\n    \"Type\":     \"Day\",\n    \"Name\":     \"Montag\",\n    \"Shortcut\": \"Mo\"\n}\n</code></pre>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#hour","level":4,"title":"Hour","text":"<pre><code>{\n    \"Id\":       \"58708f73-4703-4975-b6e2-ebb03971ff5a\",\n    \"Type\":     \"Hour\",\n    \"Name\":     \"Hauptunterricht I\",\n    \"Shortcut\": \"HU I\",\n    \"Start\":    \"07:35\",\n    \"End\":      \"08:25\"\n}\n</code></pre> <p>\"Start\" und \"End\" können auch Zeiten mit Sekunden (z.B. \"07:35:00\") sein, die Sekunden werden ignoriert. Diese Felder sind ggf. für Constraints notwendig, die Pausenzeiten oder Stundenlängen berücksichtigen, aber vor allem für die Ausdrucke.</p>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#teacher","level":4,"title":"Teacher","text":"<pre><code>{\n    \"Id\":           \"9e3251d6-0ab3-4c25-ab66-426d1c339d37\",\n    \"Type\":         \"Teacher\",\n    \"Name\":         \"Bach\",\n    \"Shortcut\":     \"SB\",\n    \"Firstname\":    \"Sebastian\",\n    \"Absences\":     [\n        {\"Day\": 0, \"Hour\": 7},\n        {\"Day\": 0, \"Hour\": 8}\n    ],\n    \"MinLessonsPerDay\": 2,\n    \"MaxLessonsPerDay\": -1,\n    \"MaxDays\":          -1,\n    \"MaxGapsPerDay\":    -1,\n    \"MaxGapsPerWeek\":   3,\n    \"MaxAfternoons\":    -1,\n    \"LunchBreak\":       true\n}\n</code></pre> <p>Bei den Min-/Max-Constraints bedeutet -1, dass der Constraint nicht aktiv ist.</p>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#subject","level":4,"title":"Subject","text":"<pre><code>{\n    \"Id\":           \"5791c199-3fa3-4aea-8124-bec9d4a7759e\",\n    \"Type\":         \"Subject\",\n    \"Name\":         \"Hauptunterricht\",\n    \"Shortcut\":     \"HU\"\n}\n</code></pre>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#room","level":4,"title":"Room","text":"<pre><code>{\n    \"Id\":       \"f0d7a9e4-841e-4585-adee-38cde49aa676\",\n    \"Type\":     \"Room\",\n    \"Name\":     \"Klassenzimmer 1\",\n    \"Shortcut\": \"k1\",\n    \"Absences\": []\n}\n</code></pre>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#roomgroup","level":4,"title":"RoomGroup","text":"<p>In Waldorf 365 hat ein Kurs eine Liste „PreferredRooms“. Von dieser Liste muss eins dieser Room-Elemente für jede Stunde (Lesson-Element) des Kurses zur Verfügung stehen. Die einzelnen Stunden können unterschiedliche Räume haben. Diese Liste kann auch leer sein. Alternativ kann die Liste aus einer RoomGroup-Referenz bestehen. Dann braucht jede Stunde alle Räume der Raumgruppe (sie sind „Pflichträume“).</p> <p>In Waldorf 365 wird eine Raumgruppe über das RoomGroup-Feld der Room-Elemente definiert. Da sie eigentlich ganz andere Objekte sind, haben sie hier einen eigenen Element-Typ.</p> <pre><code>{\n    \"Id\":       \"f0d7a9e4-841e-4585-adee-38cde49aa676\",\n    \"Type\":     \"RoomGroup\",\n    \"Name\":     \"ad hoc room group for epoch plans w2, k11, auv, ch, mu\",\n    \"Shortcut\": \"adhoc11\",\n    \"Rooms\":    [\n        \"36dd1fff-8a20-42f2-a3b6-27b244e10150\",\n        \"541ae8c8-5c31-4e80-8d08-a1935b13294e\",\n        \"63d6f30e-e064-490e-9f95-cd212eb6c435\",\n        \"d4daeb99-d562-4a41-a2ec-ca11cd2d4bca\",\n        \"d84476e9-01fa-4396-a703-cdec8fd2ec13\"\n    ]\n}\n</code></pre> <p>\"Rooms\" enthält nur Room-Referenzen (also real vorhandene Einzelräume).</p>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#class","level":4,"title":"Class","text":"<pre><code>{\n    \"Id\":           \"5b6cbd2c-d27f-4e73-8a56-a1c7d348b727\",\n    \"Type\":         \"Class\",\n    \"Name\":         \"Klasse 10\",\n    \"Shortcut\":     \"10\",\n    \"Absences\":     [],\n    \"Level\":        10,\n    \"Letter\":       \"\",\n    \"Divisions\":    [\n        {\n            \"Id\": \"9137860d-a656-400f-b4c1-d3e90cf5a4d8\",\n            \"Type\": \"Division\",\n            \"Name\": \"A und B Gruppe\",\n            \"Groups\": [\n                \"904e8c12-a817-49a1-9fc2-f554a19f5873\",\n                \"4c35be10-2519-41bb-9539-4c4caf95f8e7\"\n            ]\n        },\n        {\n            \"Name\": \"Fremdsprachengruppen\",\n            \"Groups\": [\n                \"00c1ec1b-5f65-43c1-9a73-5fff8d8751e2\",\n                \"32c960be-d2da-4cdf-ad80-6ab61f45aef6\",\n                \"cc92e228-52a2-412a-b861-de9952d87a51\"\n            ]\n        },\n        {\n            \"Name\": \"Leistungsgruppen\",\n            \"Groups\": [\n                \"b7a88739-e323-49a8-911d-7ba67cb746cd\",\n                \"0fc5740c-1706-475c-b496-ec722e8c5a58\"\n            ]\n        }\n    ],\n    \"MinLessonsPerDay\": 4,\n    \"MaxLessonsPerDay\": -1,\n    \"MaxGapsPerDay\":    -1,\n    \"MaxGapsPerWeek\":   1,\n    \"MaxAfternoons\":    3,\n    \"LunchBreak\":       true,\n    \"ForceFirstHour\":   true\n}\n</code></pre> <p>\"Shortcut“ ist eigentlich nur \"Level\" + \"Letter\", aber in dieser Form oft nützlicher.</p> <p>In Waldorf 365 ist eine Division ein Top-Level-Objekt. Deswegen haben sie ein \"Id\"-Feld. Da sie nur hier gebraucht werden, erscheinen die Division-Elemente nur im \"Divisions\"-Feld der Klassen.</p>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#group","level":4,"title":"Group","text":"<pre><code>{\n    \"Id\":           \"00c1ec1b-5f65-43c1-9a73-5fff8d8751e2\",\n    \"Type\":         \"Group\",\n    \"Shortcut\":     \"F\",\n}\n</code></pre>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#course","level":4,"title":"Course","text":"<p>Für die eigentliche Stundenplanung braucht man nur Lesson-Elemente. Die Kurselemente können aber wichtige Informationen über manche Beziehungen verdeutlichen. Anders als in Waldorf 365 steht hier ein Course-Element nur für einen „normalen“ Kurs. Die Epochenschienen werden durch die SuperCourse-Elements zusammen mit den SubCourse-Elementen (für die Epochenkurse) abgedeckt.</p> <pre><code>{\n    \"Id\":           \"c0f5c633-534a-43f5-9541-df3d93b771a9\",\n    \"Type\":         \"Course\",\n    \"Subjects\":     [\n        \"12165a63-6bf9-4b81-b06c-10b141d6c94e\"\n    ],\n    \"Groups\":       [\n        \"2f6082ce-0eb9-45ff-b2e8-a5475462454c\"\n    ],\n    \"Teachers\":     [\n        \"f24f0ed6-f5ad-423e-9a6c-6a46536b85ab\"\n    ],\n    \"PreferredRooms\":   [\n        \"541ae8c8-5c31-4e80-8d08-a1935b13294e\",\n        \"4d44ae7e-0e31-4aa0-a539-d4b2570b1b5c\",\n        \"7d0c09fa-eaf6-4298-8faa-afeb1f4477c4\"\n    ]\n}\n</code></pre> <p>Waldorf 365 unterstützt Kurse mit mehr als einem Fach. Nur deswegen ist hier \"Subjects\" eine Liste. Die \"Groups\" können Group- oder Class-Elemente sein. Für \"PreferredRooms\" siehe die \"RoomGroup\"-Beschreibung.</p>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#supercourse","level":4,"title":"SuperCourse","text":"<pre><code>{\n    \"Id\":           \"kNWJ5jArzE_hQ9FSl6pE3\",\n    \"Type\":         \"SuperCourse\",\n    \"EpochPlan\":    \"271baf6f-151b-4354-b50c-add01622cb10\",\n    \"Subject\":      \"5791c199-3fa3-4aea-8124-bec9d4a7759e\",\n    \"SubCourses\":   [\n        SubCourse-Element,\n        SubCourse-Element\n    ]\n}\n</code></pre> <p>SubCourse</p> <p>Ein SubCourse-Element ist fast genau wie ein Course-Element, darf aber nicht als \"Course\" eines Lesson-Elements vorkommen. Da die SubCourse-Element nur im Zusammenhang mit einem SuperCourse-Element relevant sind, tauchen sie nur in dessen \"SubCourses\"-Feld auf. Auch aus Gründen, die mit wiederholten Id-Feldern zu tun haben, ist ein SubCourse kein Top-Level-Element.</p> <pre><code>{\n    \"Id\":           \"c0f5c633-534a-43f5-9541-df3d93b771a9\",\n    \"Type\":         \"SubCourse\",\n    \"Subjects\":     [\n        \"12165a63-6bf9-4b81-b06c-10b141d6c94e\"\n    ],\n    \"Groups\":       [\n        \"2f6082ce-0eb9-45ff-b2e8-a5475462454c\"\n    ],\n    \"Teachers\":     [\n        \"f24f0ed6-f5ad-423e-9a6c-6a46536b85ab\"\n    ],\n    \"PreferredRooms\":   [\n        \"541ae8c8-5c31-4e80-8d08-a1935b13294e\",\"4d44ae7e-0e31-4aa0-a539-d4b2570b1b5c\",\"7d0c09fa-eaf6-4298-8faa-afeb1f4477c4\"\n    ]\n}\n</code></pre>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"W365/stundenplanschnittstelle.html#lesson","level":4,"title":"Lesson","text":"<pre><code>{\n    \"Id\":       \"sSLW2M3LKhxTjMk_MWU_h\",\n    \"Type\":     \"Lesson\",\n    \"Course\":   \"kNWJ5jArzE_hQ9FSl6pE3\",\n    \"Duration\": 1,\n    \"Day\":      0,\n    \"Hour\":     0,\n    \"Fixed\":    true,\n    \"LocalRooms\":   [\n        \"f28f3540-dd02-4c6d-a166-78bb359c1f26\"\n    ],\n    //\"Background\": \"#FFE080\",\n    //\"Footnote\": \"Eine Anmerkung\"\n}\n</code></pre> <p>\"Course\" kann ein Course- oder ein SuperCourse-Element sein. \"LocalRooms\" sind die Room-Elemente (nur reale Räume), die dem Lesson-Element zugeordnet sind. Sie sollten kompatibel mit den \"PreferredRooms\" des Kurses sein.</p> <p>Ein nicht platziertes Lesson-Element hätte:</p> <pre><code>    \"Day\":          -1,\n    \"Hour\":         -1,\n    \"Fixed\":        false,\n    \"LocalRooms\":   []\n</code></pre>","path":["W365","Waldorf 365: Schnittstelle für die Stundenplanung"],"tags":[]},{"location":"db/blockroomchoices.html","level":1,"title":"Room Choice Lists and \"SuperCourses\"","text":"<p>Handling rooms for <code>SuperCourses</code> is difficult, because there is in general not enough information available to know which rooms must be available in parallel. A <code>SubCourse</code> can specify which room(s) it needs (a <code>Room</code>, <code>RoomGroup</code> or <code>RoomChoiceGroup</code>), but because the timetabler – in the absence of a finished plan for the placement of the <code>SubCourses</code> within the year – can't know which <code>SubCourses</code> need to be allocated in parallel. Even if this information could be made available somehow, it's use can lead to potentially fragile solutions where the movement of a <code>SubCourse</code> could break the room allocations.</p> <p>Thus it is strongly recommended to use only fixed rooms for <code>SubCourses</code>. The fixed room(s) of all the <code>SubGroups</code> are taken as fixed for the <code>SuperCourse</code>. It makes no difference if a room appears twice (duplicates simply being removed).</p> <p>The room specification for <code>SuperCourses</code> is handled as follows:</p> <p>1) The rooms specified for all the <code>SubCourses</code> are combined, rejecting duplicates, whether they are fixed rooms (<code>Rooms</code> and the <code>Rooms</code> within <code>RoomGroups</code>) or the sets of <code>Rooms</code> within <code>RoomChoiceGroup</code>. The total number of rooms to be reserved is the number of unique fixed rooms plus the number of unique choice lists (the order of <code>Rooms</code> within a <code>RoomChoiceGroup</code> is irrelevant).</p> <p>Thus, in order to ensure that an extra room is reserved, either a new fixed room or a new (distinct from those of the other <code>SubCourses</code>) room-choice list must be specified for a <code>SubCourse</code>. </p> <p>At the end of this step there is a list of fixed rooms and a list of room-choice lists.</p> <p>2) The fixed rooms are eliminated from the room-choice lists. If a list now contains only one room, this list is removed from the room-choice lists and the remaining room is added to the fixed rooms (i.e. no change in total number of rooms) – and then step 2 is repeated. If a room-choice list is empty, it is removed from the room-choice lists, causing the total number of rooms to drop. If the room total drops below the expectation, an error is reported and an attempt at recovery is made (simply dropping all room-choice lists). Note that the room total can increase at step 4.</p> <p>3) The room-choice lists are then analysed by constructing their Cartesian product, eliminating result values which contain repeated rooms and duplicate values (room sets – the order of the rooms is irrelevant). With an invalid set of room-choice lists it is possible that this will lead to an empty result list. In that case, the error is reported and an attempt at recovery is made (simply dropping all room-choice lists).</p> <p>4) The individual value lists of the Cartesian product are then searched for rooms which are present in all value lists. These rooms are added to the fixed rooms (thus increasing the room total). If any changes were made, the process is started again at step 2.</p> <p>Depending on the algorithm used to \"solve\" the timetable, it may be more efficient to use a list of the final Cartesian product values than to use the list of room-choice lists.</p> <p>TODO: As this is a rather complicated algorithm, it should be checked more extensively ... does it do exactly what it is supposed to?</p>","path":["Db","Room Choice Lists and \"SuperCourses\""],"tags":[]},{"location":"db/constraints.html","level":1,"title":"Constraints","text":"<p>Some constraints are implicit (such as those preventing collisions). Others are specified along with the items they constrain, for example blocked times or number of gaps per day for teachers or classes. Others are specified in the top-level Constraints list. These often constrain the activities of one or more courses, perhaps the relationships between activities of different courses. The currently supported constraint types in the Constraints list are described below.</p>","path":["Db","Constraints"],"tags":[]},{"location":"db/constraints.html#the-constraints-list","level":2,"title":"The Constraints List","text":"<p>All constraints have a Weight field, which is an integer between 0 and 100. A value of 0 means the constraint is inactive, a value of 100 specifies a \"hard\" constraint.</p>","path":["Db","Constraints"],"tags":[]},{"location":"db/constraints.html#automaticdifferentdays","level":3,"title":"AutomaticDifferentDays","text":"<p>The activities of a course should all be on different days.</p> <p>This constraint may be specified at most once. If it is not present, a hard constraint affecting all courses is implemented. Note, however, that this constraint can be overridden for individual courses by DaysBetween constraints (see below).</p> <ul> <li>Weight (integer)</li> <li>ConsecutiveIfSameDay (boolean): In cases where the constraint is not fulfilled, the activities should be consecutive. This is only relevant if the constraint is soft, but if it is true this part of the constraint is always hard!</li> </ul>","path":["Db","Constraints"],"tags":[]},{"location":"db/constraints.html#daysbetween","level":3,"title":"DaysBetween","text":"<p>The activities of a course should have at least the given number of days between them (where a value of 1 is equivalent to different-days).</p> <ul> <li>Weight (integer)</li> <li>Courses (list of Course or SuperCourse references): The constraint will apply to each of the listed courses.</li> <li>DaysBetween (integer)</li> <li>ConsecutiveIfSameDay (boolean): If the activities do end up on the same day, they should be consecutive (always hard!).</li> </ul> <p>If DaysBetween is 1, this constraint will override AutomaticDifferentDays for the listed courses. Otherwise it is a distinct constraint.</p>","path":["Db","Constraints"],"tags":[]},{"location":"db/constraints.html#daysbetweenjoin","level":3,"title":"DaysBetweenJoin","text":"<p>This constraint applies between the individual activities of the two courses, not between the activities of a course itself. That is, between course 1, activity 1 and course 2 activity 1; between course 1, activity 1 and course 2, activity 2, etc. Otherwise it is similar to the DaysBetween constraint.</p> <ul> <li>Weight (integer)</li> <li>Course1 (Course or SuperCourse reference)</li> <li>Course2 (Course or SuperCourse reference)</li> <li>DaysBetween (integer)</li> <li>ConsecutiveIfSameDay (boolean): If the activities do end up on the same day, they should be consecutive (always hard!).</li> </ul> <p>NOTE: The <code>ConsecutiveIfSameDay</code> flag should be respected by all the \"days-between\" constraints even if the weight is 0.</p>","path":["Db","Constraints"],"tags":[]},{"location":"db/constraints.html#activitiesendday","level":3,"title":"ActivitiesEndDay","text":"<p>The activities of the specified course should be the last activities of the day for the student group concerned.</p> <ul> <li>Weight (integer)</li> <li>Course (Course or SuperCourse reference)</li> </ul>","path":["Db","Constraints"],"tags":[]},{"location":"db/constraints.html#afterhour","level":3,"title":"AfterHour","text":"<p>The activities of the specified courses should lie after the given hour (excluding the given hour).</p> <ul> <li>Weight (integer)</li> <li>Courses (list of Course or SuperCourse references): The constraint will apply to each of the listed courses.</li> <li>Hour (integer)</li> </ul>","path":["Db","Constraints"],"tags":[]},{"location":"db/constraints.html#beforehour","level":3,"title":"BeforeHour","text":"<p>The activities of the specified courses should lie before the given hour (excluding the given hour).</p> <ul> <li>Weight (integer)</li> <li>Courses (list of Course or SuperCourse references): The constraint will apply to each of the listed courses.</li> <li>Hour (integer)</li> </ul>","path":["Db","Constraints"],"tags":[]},{"location":"db/constraints.html#parallelcourses","level":2,"title":"ParallelCourses","text":"<p>Die activities of the specified courses should start at the same time. The number and duration of the activities must be the same in all the courses.</p> <ul> <li>Weight (integer)</li> <li>Courses (list of Course or SuperCourse references): The constraint will apply to each of the listed courses.</li> </ul> <p>TODO: The following constraints are not yet finally specified. It might be better to limit them to particular classes or to specify them in some other way.</p>","path":["Db","Constraints"],"tags":[]},{"location":"db/constraints.html#doubleactivitynotoverbreaks","level":2,"title":"DoubleActivityNotOverBreaks","text":"<p>This constraint should be present at most once. A double-lesson should not cover a break time. The breaks are specified by the \"Hours\" list, the breaks being immediately before the given hours.</p> <ul> <li>Weight (integer)</li> <li>Hours (list of integers)</li> </ul>","path":["Db","Constraints"],"tags":[]},{"location":"db/constraints.html#minhoursfollowing","level":2,"title":"MinHoursFollowing","text":"<p>There should be at least the given number of hours between the end of the activities of the first course and the start of those of the second course, if they are on the same day.</p> <ul> <li>Weight (integer)</li> <li>Course1 (Course or SuperCourse reference)</li> <li>Course2 (Course or SuperCourse reference)</li> <li>Hours (integer)</li> </ul>","path":["Db","Constraints"],"tags":[]},{"location":"db/prepare_timetable_backend.html","level":1,"title":"Preparing for a timetable-generation run","text":"<p>Although the project currently goes under the name \"fetrunner\", one of its aims is to be able to support generation back-ends other than <code>FET</code>, eventually, by means of an interface consisting of function signatures.</p> <p>Note that at present, when the input file is a <code>FET</code> file, no database is built. The \"autotimetable\" algorithm works directly on the XML data structure of the input file. In a way this reflects the initial impulse for <code>fetrunner</code>, the database structures are an addition to allow input data from other systems. <code>FET</code> supports many more constraints than the current database, and the ones they do have in common may not always be directly equivalent. <code>FET</code> also has a more flexible notion of student groups, which may be difficult to \"translate\".</p> <p>So although it might be possible to read a <code>FET</code> input file into the database structures, some information might be lost and – if using the <code>FET</code> back-end – it would be superfluous.</p> <p>TODO: Detail the differences for the \"autotimetable\" functions when using <code>FET</code> input vs. database data.</p> <p>The information relevant for timetabling needs to be extracted from the database and the structures used by the back-end need to be built.</p>","path":["Db","Preparing for a timetable-generation run"],"tags":[]}]};